# objects/ — noun cards

**Reads:** AFFiNE source tree (`packages/**`, `blocksuite/**`), cited per card.
**Writes:** one card per durable noun, clustered by how an editor asks about it (not by folder layout).
**Human check:** every citation resolves to real source; `status: verified` only with date + commit; _If you change this_ waterfalls are first-order and tested against the tree.

Start at `_index.md`. Do not slurp this folder — the index exists so you don't.
